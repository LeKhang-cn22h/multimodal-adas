# Session 2026-07-14 — Gộp Seatbelt vào Driver-Service + Test YOLOv8m

## 1. Tóm tắt

Phiên làm việc xoay quanh 2 chủ đề chính:

### A. Gộp Seatbelt Detection vào Driver-Service (chưa implement, đã có 3 artifact)

Người dùng yêu cầu gộp `seatbelt_service` vào `driver-service`, đổi input
từ webcam sang video file local, để chạy đồng thời cả 2 model AI
(MediaPipe drowsiness + YOLO seatbelt) trên cùng frame.

Đã hoàn tất 3 bước đầu của quy trình AGENTS.md:

| Bước | Artifact | Trạng thái |
|---|---|---|
| Requirement | `.opencode/knowledge/requirements/REQ-merged-monitoring.md` | ✅ Done |
| Feature | `.opencode/knowledge/features/FEAT-merged-monitoring.md` | ✅ Done |
| Tech Solution | `.opencode/knowledge/tech-solutions/TS-merged-monitoring.md` | ✅ Done |
| Implement | — | ❌ Chưa làm (người dùng bảo dừng) |

**Điểm quan trọng trong thiết kế**:
- **Không tạo service mới** — sửa trực tiếp `driver-service`.
- **Không sửa logic AI gốc** của cả 2 service (MediaPipe pipeline + YOLO detector),
  chỉ thêm file mới và sửa orchestrator/config/api.
- 4 file mới: `seatbelt_detector.py`, `video_file_reader.py`,
  `overlay_renderer.py`, `best_1.pt`.
- 4 file sửa: `orchestrator.py`, `config.py`, `publisher.py`, `fatigue.py`.
- 4 thread: MainThread (FastAPI), capture (video/webcam), ai-worker (dual model),
  display (cv2.imshow).

**Người dùng sau đó quyết định**: "bỏ qua cho tôi chức năng gộp service đi".

### B. Test model YOLOv8m mới train

Người dùng có model YOLOv8m vừa train tại:
`seatbelt_service/app/seatbelt_yolov8m/weights/best.pt` (~50MB, mAP@0.5 = 0.705).

Yêu cầu: viết script chọn video từ local để xem kết quả detection.

**Đã làm**: Cập nhật `main_detection.py`:
- Đổi model path sang `app/seatbelt_yolov8m/weights/best.pt`.
- Thêm FPS counter, detected classes list trên overlay.
- Thêm detection summary (thống kê từng class) khi xử lý xong.

## 2. Quyết định đã chốt

- **Gộp seatbelt vào driver-service**: Tạm dừng, chưa implement.
- **Model YOLOv8m**: Class vẫn là 7 class cũ (cell phone, drinking, eyeglass,
  hands off, hands on, mask, seatbelt), class_id=6 là seatbelt.

## 3. Files bị ảnh hưởng

| File | Hành động |
|---|---|
| `.opencode/knowledge/requirements/REQ-merged-monitoring.md` | Tạo mới (cập nhật 2 lần) |
| `.opencode/knowledge/features/FEAT-merged-monitoring.md` | Tạo mới |
| `.opencode/knowledge/tech-solutions/TS-merged-monitoring.md` | Tạo mới |
| `services/seatbelt_service/main_detection.py` | Sửa (đổi model + cải tiến overlay) |

## 4. Open questions

Không có câu hỏi mở mới.
